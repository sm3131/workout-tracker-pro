<h2 class="h-text pt-2 text-center">Community Workouts</h2>
{{#if workouts.length}}
<ul class="container workout-list">
  {{#each workouts as |workout|}}
  <li class="col-12">
    {{> community-workout-info workout }}
  </li>
  {{/each}}
</ul>
{{else}}
<h3 class="h-text pt-2 text-center">No Workouts to Display! Be the First to Post a Workout!</h3>
{{/if}}